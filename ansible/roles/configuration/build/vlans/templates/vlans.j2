#jinja2: lstrip_blocks: "True (or False)", trim_blocks: "True (or False)"
{#- ---------------------------------------------------------------------------------- #}
{# vlans                                                                               #}
{# ---------------------------------------------------------------------------------- -#}
{% if route_or_switch == "switch" %}
vlans {
    trust {
        description "DMZ clean vlan";
        vlan-id {{ vlan_trust }};
    }
    untrust {
        description "WAN dirty vlan";
        vlan-id {{ vlan_untrust }};
    }
}
{% endif %}